#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    const double pi = 3.14;
    double r;
    cin>>r;

    cout<<pi*pow(r,2)<<endl;
    cout<<2*pi*r;
    return 0;
}